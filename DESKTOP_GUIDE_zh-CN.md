# Codex Glass 4.0 · 使用指南

## 启动

运行 `CodexGlass.exe`；源码运行 `python desktop_widget.py`。复用已有 Conda 环境即可，桌面依赖见 requirements-desktop.txt。重复启动会恢复现有组件。

默认连接本机 127.0.0.1:8081，服务不存在时自动启动后台。旧版服务已占用端口时，应先正常停止旧服务再升级，不同时运行两个索引后台。Windows EXE 无需安装 Python。

## 小窗口

- 标题空白处拖动位置；拖动窗口边缘 / 四角等比例缩放内容。
- 下方箭头收起为迷你窗口；迷你窗口保留上箭头用于展开。
- 点击图钉切换置顶。更多菜单提供外观、动态效果、立即检查、完整看板、托盘收起与退出。
- 点击托盘图标恢复；关闭窗口默认收起，不删除数据。
- 今日 / 5 小时 / 累计只改变用量统计范围。模型小曲线来自真实时间桶，点数取决于现有数据。
- 悬停主曲线查看采样时间与数值；在图表范围内滚轮缩放，双击复位。点击模型查看输入、缓存、输出和推理。

## 完整看板

从小窗口或托盘菜单打开“完整看板”，直接显示原生窗口，不打开外部浏览器。关闭看板不会退出小窗口。

- 总览：查看指标、用量趋势、价格估算和额度摘要。
- 模型：模型 / 匿名工作区切换；搜索和多选筛选；点击行查看 Token 结构。
- 历史：时间范围、搜索、多选模型、表头排序及分页。CSV 仅导出当前显示的一页，文件名由你选择。
- 额度：当前本机账号的有效快照，不随历史用量范围变化。过期或缺失窗口不会补造。
- 设置：透明度、预设、流彩、置顶、图表动画、缩放与本地索引概况。

图表缩放不改变累计费用或 Token 事实。历史区间显示实际日期 / 时间，不冒充“现在”。窗口缩放改变整个界面大小，而非裁掉内容。

## 导入另一台电脑

选择“导入 SQLite”，选取解压后的索引并确认。程序验证格式、备份目标、自动去重，再触发汇总刷新；不会在后台自动导入未知文件。

导入前请保证源 SQLite 是一致性备份。如果源后台仍在运行，不能只复制主数据库而忽略 WAL。重复导入相同事实不会增加计数。源索引不会被写入。

同一目录内生成的 `*.before-import-*.sqlite3` 是导入前备份，请自行保留。数据库可能含工作目录和会话文件路径，不应上传公开仓库。

## 费用和额度

费用是当前模型价格的美元估算，未知价格显示“未计价”，不是 OpenAI 实际账单或订阅余额。缓存计入输入、推理计入输出，不重复加总。

本机默认索引是 `~/.codex/codex-monitor.sqlite3`。账号额度来自本机最新官方记录，导入的旧快照不替代当前账号。Pro 优先周额度，Plus 按有效记录显示五小时 / 周窗口。没有记录的窗口不显示；不从套餐名称推测真实剩余额度。

## 性能与外观

界面每五秒检查；首次大历史汇总可能需要几十秒。无新事实时复用紧凑历史并按分钟时钟更新。手动检查可安排重算；后台忙时不会让 UI 同步等待。

玻璃效果为程序化透明漫射、流彩与高光，并非系统桌面实时高斯模糊。背景仅在圆角边界内绘制。隐藏窗口停止无用动画；关闭动画可进一步降低绘制开销。

位置 / 外观设置保存在当前用户的 CodexMonitor 注册表配置中；自动后台日志位于 `%LOCALAPPDATA%/CodexGlass/backend.log`。卸载只需退出并删除程序文件，不要误删 `.codex/sessions` 或历史数据库。

## 排查

- 等待用量记录：先在本机使用 Codex 产生会话，再检查数据。
- 导入按钮不可用：需要连接支持本机原生导入上下文的新版本后台，不支持把远程 URL 当成本机数据库。
- 无有效额度：等待新的官方会话快照；导入旧记录不能生成当前账号额度。
- 数据较旧 / 离线：检查服务、端口和日志；界面保留上次数据，不以 0 覆盖。
- EXE 安全提示：当前发行物未签名，请从本仓库核对 SHA-256，勿关闭安全防护。
