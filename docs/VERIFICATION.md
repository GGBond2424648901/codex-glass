# 4.1 验收与复现

验证环境：Windows x64，Python 3.13、PyQt5 5.15.11 / Qt 5.15.2，PyInstaller 6.22.2。测试使用隔离临时库，性能测试使用历史备份，不对生产数据库执行清空或重导入。

## 自动化验证

2026-09-09 发布候选：**225 项 Windows 测试全部通过**。保留 4.0 的 212 项回归覆盖，增加包导入、资源定位、后台构建标识、非项目目录启动与 Qt 退出生命周期等 13 项检查。Black 格式检查通过，共 57 个 Python 文件。

```powershell
$env:QT_QPA_PLATFORM = 'windows'
python -m unittest discover -s tests -v
python -m black --check codex_glass tests monitor.py web_dashboard.py desktop_widget.py
python -m tests.capture_release work/captures
```

覆盖解析与增量索引、断点恢复、原始事实重新计价、历史导入 / 重复去重 / 回滚 / 备份、范围筛选后分页、CSV 公式防护、紧凑历史、原生五页 × 三档范围、模型筛选、范围缩放、玻璃菜单、多选、浮层、窗口缩放、托盘与展开，以及圆角外侧透明。

初始加载与测得零值分开；索引异常 / 等待与正常连接分开；离线保留观测值但关闭实时指示；额度快照超过五分钟提示较旧。

托盘和桌面合成测试需要交互式 Windows，会在无桌面环境与 offscreen 模式下失去系统托盘 / 桌面像素，不能把这种环境当作实际 Windows 外观验收。

## 视觉验收

4.1 源码整理额外执行了重构前后对比：固定演示数据、时钟并停止动画，15 个完整看板状态与 4 个小窗口状态共 19 张原生控件截图的 RGBA 像素一致。核心计算、展示数据投影与三个存储模块在排除导入语句后，Python AST 也保持一致。此检查用于证明本次文件重组没有改变既有界面和统计逻辑，不代表 AI 设计图与程序逐像素一致。

15 张页面截图来自实际原生控件，共享五个页面和三档数据，不是 15 张静态图片。另捕获多选弹层、曲线范围、三个小窗口范围、迷你窗口、菜单、外观与缩放状态。

截图全部使用匿名演示数据。重点检查表头无不透明白块、圆角外 alpha 为 0、曲线下方渐变、文字层级、控件勾选 / 悬停 / 聚焦与屏幕限制。材质通过动态位相改变像素，隐藏时动画定时器停止。

AI 设计图中的数据与文字不总是算术一致，真实程序以事实汇总为准，不为了对齐图片制造数字。字体栅格、显示器 DPI、动态材质位相与新增的真实状态提示会产生视觉差异，因此不声明全平台逐像素 100% 相同。

## 打包验证

冻结后的 EXE 在独立本机端口和临时索引上启动后台，验证 195 Token / 2 事件的已知测试样本、每页 1 条历史与本机原生上下文接口；使用冻结 EXE 捕获组件画面，确认 Qt / 资源 / 后台均被收录。

4.1 最终 EXE 连续六次完成上述启动、读取、截图、退出检查，退出码均为 0，HTML 资源与源码字节一致，截图为 440 × 686 且圆角外透明。发布前发现的退出缺陷已补充真实 Qt 进程回归：提示浮层先被销毁时的 Hide / Leave 回调，以及截图模式、普通缩放窗口、完整看板已打开三条退出路径。应用在退出前按 Qt 所有权顺序先销毁根窗口，避免把清理留给不确定的解释器析构顺序。

发布目录仅从源码白名单组装。排除 SQLite、sessions、个人配置、日志、令牌、虚拟环境、内部开发报告和原始截图中的私人桌面。保留依赖许可、对应版本源码链接与构建脚本。

## 手工验收顺序

1. 运行 EXE，小窗口可拖动、置顶、收进托盘并恢复。
2. 收成迷你模式，用上箭头展开；靠近屏幕底部展开时主体仍在屏幕内。
3. 拖动窗口四边 / 四角缩放；完整看板从原生菜单打开，关闭后小窗口仍保留。
4. 切换五页与三个范围，查看模型搜索、多选、历史分页及排序。
5. 在主图滚轮缩放、悬停、双击复位；在模型行打开详情。
6. 调整透明度 / 预设，切换流彩和图表动画，检查圆角外没有彩色矩形。
7. 如需导入，使用另一台电脑的一致性 SQLite 备份；重复导入不重复计数，保留自动备份。

大历史冷汇总、后台 RSS 与相同事实校验见 [性能说明](PERFORMANCE.md)。EXE 未签名，发布 SHA-256 供核对。
