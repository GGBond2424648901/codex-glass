# Changelog

## Unreleased

## 4.1.7 - 2026-09-09

- 迷你额度数字从 16px 提升至 22px，圆环直径从 27px 提升至 34px、线宽从 5px 提升至 7.5px。
- 展开模式放大加粗剩余百分比，保留说明文字的次级层级；剩余不超过 20% 时，数字、圆环和进度条统一为琥珀色系。
- 保留 4.1.6 的拖动手柄、高 DPI 工作区适配和 55%–150% 小窗口缩放。

## 4.1.6 - 2026-09-09

- 原生缩放宿主新增右下角可见拖动标记，边缘命中范围从 8 增至 12 个逻辑像素，悬停显示缩放指针与比例提示。
- 修正边缘按下后未移动也会缩小的跳动；拖动按鼠标位移计算，保持等比例与对侧锚点。
- 小窗口首次启动默认 85%（约 374 × 583 逻辑像素），最小允许 55%（242 × 377）；按 DPI 换算后的屏幕工作区收敛尺寸，跨屏切换时检查边界。保留尺寸记忆，不超出屏幕时不覆盖已有比例。

## 4.1.5 - 2026-09-09

- 额度窗口以本机实际官方快照为准，移除 Pro 强制过滤五小时窗口的逻辑；不凭套餐名称创造不存在的窗口。
- 对旧版 Pro 额度缓存进行有界回读恢复，不重建 Token 索引；原日志不可用时保留已有快照，等待新记录。
- 小窗口标记超过五分钟的旧快照、异常/未知时间；悬浮详情包含每个窗口的快照与重置时间，额度提示随定时刷新更新。
- 小窗口当前统计范围存在未定价模型时显示“部分预估”；小窗口和完整看板提供标准文本计价范围说明，不将估算金额宣称为实际账单。

## 4.1.4 - 2026-09-09

- 桌面统计的 SQL 读取和重新计价改为紧凑列式事件缓冲，不再长期保留每条记录的 Python 事件/Token 对象。
- 历史明细共享相同价格维度，避免每条历史重复存储三项价格；长上下文与自定义价格仍按真实维度分别保存。
- 合并工作区、时间范围与近期调用的重复遍历；近期列表使用最多 50 条记录的堆，不再排序全部事件副本。
- 紧凑统计路径不再读取本不会使用的大 JSON 缓存。保留完整历史、筛选、去重、原有价格和窗口计算口径。
- 增加内存预算、时间/数值无损往返、稳定排序、重复价格及混合来源一致性回归测试。

## 4.1.3 - 2026-09-09

- Windows 使用打开的文件句柄获取稳定卷序列号与文件索引，兼容旧 Python 的持久化身份；同时接受当前运行时原生身份进行无损迁移，不再仅因设备号宽度变化而删除该文件的索引事实。
- 保留文件替换、截断与内容变化检测；身份读取失败时保留既有事实并报告读取失败，不以不兼容身份回退重建。
- 汇总因其他写入失效后，下一次重试保持索引租约，并用独立线程续租直至完成发布；保留 generation 校验，避免无限竞争或发布已失效统计。
- 升级不清空 SQLite，不自动结束 Python/Codex 进程。请退出旧 Codex Glass EXE；遗留监控 Python 后台应核对命令行后停止，不能结束所有 Python。旧后台的高 CPU/内存不会被新 EXE 自动消除。
- 新版退出原因仍需另一台电脑验证；本版针对已复现的身份兼容与汇总竞争缺陷，不宣称覆盖所有空白或退出原因。

## 4.1.2 - 2026-09-09

- 将完整统计发布与历史明细整理分开；费用、Token、模型与图表先可用，历史明细随后完成，不再一起阻塞首屏。
- 扫描结束后明确显示 `aggregating`，诊断提供读取事实、额度整理、计价、统计、历史阶段及耗时；扫描文件数与汇总文件数分别显示。
- 将跳过格式异常记录的非致命警告与索引失败分开，保留真正的读取/数据库错误提示。
- 没有导入事实时不再为全部本机用量/额度计算跨电脑去重指纹；存在导入时保留原有去重规则。
- 桌面后台增加版本握手与 PID 信息，自动启动模式不再将旧版本后台当作新版使用。
- 增加慢历史处理时先显示统计、最终历史就绪、警告、版本和本机无导入路径的回归测试。

## 4.1.1 - 2026-09-09

- 修复自定义 `CODEX_HOME` 未用于会话、监控配置和索引路径的问题；无自定义时保留当前用户 `.codex` 路径，不按安装盘符或工作任务文件夹猜测。
- 桌面自动启动后台时显式对齐监听地址和端口，避免旧 Web 配置使小窗口连接错误端口。
- 本机端口被无关服务占用、返回非监控数据时，自动切换空闲回环端口启动后台；不终止其他程序。
- 小窗口区分连接、扫描、索引错误、没有记录和当前时段无用量，增加“复制数据诊断”；不自动清空索引或导入未知数据库。
- 置顶图标增加倾斜、深色实心状态，取消置顶恢复直立线框；菜单、窗口标志与提示同步。

## 4.1.0 - 2026-09-09

- 整理源码为 `codex_glass` 分层包，区分核心计算、存储、服务、桌面和命令行；保留三个根目录启动入口。
- 统一 Python 格式并增加开发格式配置、架构目录说明，减少密集单行代码。
- 将兼容 Web 页面的 HTML 从服务 Python 文件中独立为资源，统一源码与 EXE 的资源定位。
- 增加包导入、跨工作目录启动、资源与打包相关回归检查；本次不修改数据库结构、计价逻辑和界面设计。
- 保留 4.0 下载与本地历史，发布新的 EXE 和源码归档；不上传原始设计稿。
- 修复退出时曲线访问已销毁提示浮层的异常；统一普通窗口 / 截图模式的根窗口销毁顺序，避免退出阶段偶发访问异常，并增加生命周期回归测试。

## 4.0.0 - 2026-09-09

- 原生完整看板：总览、模型、历史、额度、设置；共享今日 / 5 小时 / 累计三档统计，不再打开浏览器。
- 与悬浮组件共用动态流彩玻璃、渐变曲线、原生菜单、多选与可读信息浮层；清除表头白块及圆角外背景。
- 拖动窗口边缘等比例缩放、迷你模式快捷展开、托盘恢复；隐藏后暂停动画。
- 历史筛选后分页与数值排序、CSV 导出、SQLite 安全导入及去重；自动等待短暂的后台扫描锁。
- 紧凑列式历史与无变化复用，大历史库显著降低内存；保留原始 Token 事实与估算合计。
- 加载、离线、索引异常和旧额度快照有独立状态，不把缺失数据当作实时零值。

## 2.6.0 - 2026-09-08

- 新增 Codex Glass 原生 Windows 透明玻璃桌面组件：默认置顶、托盘恢复、迷你折叠、透明度和位置记忆。
- 今日 / 近 5 小时 / 累计用量与预估费用；全部模型可筛选，显示采样新增和曲线、输入/缓存/输出/推理明细。
- 额度仅使用最新本机官方记录，读取同一条记录的主/次窗口；Pro 显示周额度，Plus 按实际记录显示五小时/周窗口，不拼接旧账号额度，不显示已过期窗口。
- 新增 `/api/widget` 轻量接口；兼容旧 SQLite，通过已索引字节偏移读取单条本机记录补齐第二额度窗口，无须重建历史。
- 修正最近时间窗口包含未来时间事件的问题；历史导入来源变化时重置桌面增量基线，避免误报新增。
- 保留 2.5.0 的永久 SQLite 历史导入、自动去重及重新计价功能。

## 2.5.0 - 2026-09-08

- 新增 `import-index`、`list-imports` 和 `remove-import`，可永久导入另一台电脑导出的 SQLite 历史索引并管理来源。
- 使用跨电脑稳定会话/事件指纹自动去重；本机事实优先，重复导入保持幂等，导入历史会随当前定价统一重新计价。
- 导入前只读验证源库并自动备份目标，导入或删除失败时事务回滚；普通本机索引重建不会清除外部历史。
- 仪表板新增导入来源、文件和事件数量提示，并继续隐藏导入数据库及会话文件的完整路径。
- 修复 Python 3.13 对超深 JSON 数组解析更宽松时，非对象 `payload` 未被记录为格式异常的问题。

- Web 默认使用持久化 SQLite 索引，首次导入全部本地会话日志，后续增量读取追加字节，并保存可恢复的文件读取位置。
- 新增首次导入进度提示、`/healthz`、`/api/index-status` 和数据响应中的 `index` 状态；索引期间总计明确标为不完整，服务可在全量导入完成前启动。
- 新增 `index-status`、`--index-db`、`--rebuild-index` 和 `--no-index`，支持只读检查进度、自定义索引路径、重建和原始解析诊断模式。
- 补充本地历史覆盖范围、部分/完整统计语义、索引隐私和自动化验证说明。

## 2.4.3 - 2026-03-19

- 新增 README 首页实拍截图，并补齐 `docs/images` 资源，提升公开展示效果。
- 进一步优化仓库首页结构与说明文案，让开源页更适合直接传播和新用户快速理解。
- 补上正式 GitHub Release 所需的版本对齐工作，统一版本、标签与发布说明口径。

## 2.4.2 - 2026-03-19

- 补齐开源发布所需的仓库文档：定位、亮点、安装、启动、截图占位、配置、FAQ 和开源友好说明。
- 新增 `monitor_config.example.json`，方便新用户直接复制本地配置并调整模型别名与计费覆盖。
- 新增主目录 `.gitignore` 和 MIT `LICENSE`，并统一去除个人身份暴露风险。

## 2.4.1 - 2026-03-19

- 将右侧悬浮 drill-down 摘要并入主栅格，改为默认可见的情报面板，消除宽屏时右侧大块留白和漂浮感。
- 修正右侧情报列在 CSS Grid 中被隐式列压窄的问题，让摘要面板与态势侧栏稳定占满整列宽度。
- 保留 hover drill-down 交互，但默认回到实时摘要，不再把关键右侧信息藏在悬浮态里。

## 2.4.0 - 2026-03-19

- 为 Web 监控台加入更高级的实时切换过渡、数值刷新高亮、告警层级动画和悬停 drill-down 面板。
- 新增大屏模式，支持按钮切换和键盘快捷键 `F` / `Esc`，便于长时间挂屏监控。
- 将“累计花费”提升到首屏核心信号位，并保留战术卡中的总费用展示，避免总金额不够显眼。
- 启动输出现在会直接提示如何关闭：前台模式提示 `Ctrl+C`，后台模式提示 `python3 monitor.py stop`，并新增 `close` 别名。
- 收束首屏骨架，减弱重复信息和漂浮面板感，把金额显示改为更克制的监控台排版。

## 2.3.1 - 2026-03-19

- 补强启动链路：后台模式现在会校验现有服务是否健康，不健康时自动重启，避免提示“已运行”但页面实际打不开。
- 新增 `python3 monitor.py open`，用于重新打开已运行的仪表板；如果服务未启动，会自动尝试拉起。
- 后台启动健康检查改为带重试的等待逻辑，失败时明确提示日志路径和前台排查命令。

## 2.3.0 - 2026-03-19

- 进一步重构 Web 监控中心，把首屏从卡片墙收束为更接近指挥台的战术面板、主图焦点和态势解释侧栏。
- 修正 5 小时配额快照选择逻辑，避免模型级 `rate_limits` 快照把更有意义的全局 `codex` 窗口快照覆盖掉。
- 补齐前端与后端监控字段映射，让 5 小时配额、异常等级、活跃模型数、最新活动和窗口粒度使用统一口径。

## 2.2.0 - 2026-03-19

- 重构 Web 监控中心的上半屏信息层级，让 hero、KPI、主图和侧栏更接近高级系统监控台的视觉结构。
- 移除页面中所有会直接展示本机路径来源的内容，包含会话目录、目录来源和工作目录的原始路径输出。
- 将工作区相关展示统一改为匿名化标签，并把终端启动日志里的路径输出改为通用描述，降低开源泄露风险。

## 2.1.1 - 2026-03-19

- 为 Web 监控中心增加进入动画、扫描线氛围、卡片悬浮反馈和表格位移动效，让界面更接近实时监控台。
- 为趋势图、模型柱状图、令牌环图和 5 小时热区条带增加鼠标悬停交互，支持高亮、交叉线和 tooltip。
- 增加 `prefers-reduced-motion` 兼容逻辑，在系统要求减少动画时自动关闭主要动效。

## 2.1.0 - 2026-03-18

- 将 Web 仪表板重构为更接近系统监控中心/NOC 的深色监控台界面，强化窗口健康、实时负载和事件流展示。
- 为最近 5 小时窗口新增 5 分钟滚动槽位聚合 `windows.last_5_hours.by_slot`，让主图和热区条带比按小时聚合更准确。
- 新增 `metrics` 监控指标，包括缓存命中率、输出占比、5 小时与 15 分钟吞吐、活跃模型数、活跃目录数、最近事件新鲜度和异常级别。
- 修正 `rate_limits` 最新快照选择逻辑，新增 `observed_at` 和 `scope` 字段，避免把事件时间错误地与重置时间比较。
- 为事件明细的费率拆分补上 long context 定价修正，确保与主统计口径一致。

## 2.0.1 - 2026-03-18

- 修复 `python3 monitor.py enhanced --once` 因 Rich markup 字符串错误而崩溃的问题。
- 更新内置 OpenAI 模型定价，补充 `gpt-5.4`、`gpt-5.4-mini`、`gpt-5.4-nano`、`gpt-5.4-pro` 等新模型。
- 为 `gpt-5.4` / `gpt-5.4-pro` 增加 long context 定价规则支持。
- 刷新 `gpt-realtime-mini`、`gpt-realtime-1.5`、`gpt-audio-1.5`、`gpt-image-1.5`、`gpt-image-1-mini` 的内置单价。
- 新增 `requirements.txt`，明确增强终端模式所需的 `rich` 依赖。
